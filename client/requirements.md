## Packages
framer-motion | Page transitions and premium smooth animations
recharts | Dashboard analytics and mock trading charts
lucide-react | High-quality icons for UI elements
clsx | Class name merging
tailwind-merge | Tailwind class merging utility

## Notes
- Assumes `client/src/hooks/use-auth.ts` exists and exports `useAuth` (as generated by Replit integration)
- Uses deep dark mode ONLY (no light mode toggle needed for this brand aesthetic)
- Relies on standard Drizzle `numeric` type being handled as strings or coerced properly by the backend
